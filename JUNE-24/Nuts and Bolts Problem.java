class Solution {
    void matchPairs(int n, char nuts[], char bolts[]) {
        // The Arrays.sort method is used to sort the nuts array in ascending order.
        Arrays.sort(nuts);

        // The Arrays.sort method is used to sort the bolts array in ascending order.
        Arrays.sort(bolts);
    }
}
